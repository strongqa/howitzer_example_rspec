language: ruby
rvm:
  - 2.3.1
script:
  - bundle exec rake features:smoke
  - OPTS="driver=sauce" bundle exec rake features:smoke
  - OPTS="driver=testingbot" bundle exec rake features:smoke
  - OPTS="driver=browserstack" bundle exec rake features:smoke
  - bundle exec rake features:bvt features:p1 features:p2